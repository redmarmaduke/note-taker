{
  "name": "note-taker",
  "version": "1.0.0",
  "description": "Toy note taker application where I built the code to handle event handling for the page including note creation and deletion.",
  "main": "index.js",
  "scripts": {
    "start:dev": "nodemon server.js",
    "start:prod": "node server.js",
    "start": "if-env NODE_ENV=\"production\" && npm run start:prod || npm run start:dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Manuel Nunes <manuel_s_nunes@yahoo.com> (https://github.com/redmarmaduke)",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "if-env": "^1.0.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
